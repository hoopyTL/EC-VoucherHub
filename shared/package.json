{
  "name": "@voucher/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force",
    "typecheck": "tsc -b --force"
  }
}

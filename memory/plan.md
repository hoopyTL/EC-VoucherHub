# Plan

> Project-wide task breakdown. Generated by the `planning` skill from `docs/02-srs/` +
> `docs/06-architecture/` (+ 05/07/08). Lives in `memory/` (agent state), not in `docs/`.
> Tasks are numbered continuously over time — do NOT reset per feature.
> Current working state (which task is active) → `memory/active.md`.

---

## Tasks

### TASK-001: [short name]
**Source**: [FR-X / FLOW-X in docs/02-srs]
**Scope**: [1-2 sentences]
**API Contract** (if API task): → see docs/07-api-design
| Method | Path | Request | Response |
|--------|------|---------|----------|

**DB Schema** (if DB task): → see docs/05-database-design
```sql
...
```

**UI Spec** (if UI task): → see docs/08-frontend-design
- Components + data-testid

**Acceptance criteria** (copy from docs/02-srs, do not paraphrase):
- [ ] ...

**Docs to sync**: [which deliverable to update in the same change — e.g. 07-api-design, 05-database-design]

**Dependencies**: — / TASK-XXX

**Implement via**: write code / `migration` skill / `system-design` skill

---

### TASK-002: ...
...

---

## Implementation order

1. TASK-001 — no dependencies
2. TASK-002 — depends on TASK-001
3. ...

## Project-wide notes

- [shared gotchas]
- [tech constraints to remember while implementing]
